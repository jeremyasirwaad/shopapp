import 'package:flutter/material.dart';

class listcard extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return GridTile(child: Container(child: Image.network(  ),));
  }             
}